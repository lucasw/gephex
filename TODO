
was die max leute sich wuenschen<martin>
	Wandsteuerung
	
  // Previewmodus fuer GDI Out *FERTIG, coma
	
  Tastaturkuerzel:
		Wandmodi (das was früher auf den F-Tasten war)
		Ausgabewindowposition (normal oder preview)
		Snapshots
		Sequenzen
	Ketten von Sequenzen = Playlist ? (Georg)
	// komplexeres Fontmodul * KÜMMER MICH DRUM, coma

   // beim verbinden alte verbindung loeschen * FERTIG, sagt georg :)
  // groessere inputs und outputs //Start->Systemsteuerung->Eingabehilfen->FürBlödis
  unnoetige module/Controlls/Menuoptionen entfernen (z.B. reconnecten)
		eventuell expert/novice mode oder sowas

  -module koennen ihren status in der gui anzeigen, also z.b. bild im
   bmpmodule...

gui:
	-widget fuer serial out!!!


model:
	copy graph in existierenden graph
	copy snapshot in existierenden snapshot
	hab ich mir nochmal überlegt, brauchts eigtl. nicht.
	dafür muss aber ne überprüfung rein, daß copy nicht gemacht wird, wenns
	den graph/snapshot schon gibt

seqeuncer:
	wenn man einen graphen oder snapshot umbenennt,
	was passiert dann mit den sequencen, die diesen graphen/snapshot
	verwenden?

engine:
	irendererstatusreceiver und imodelstatusreceiver verwenden

model:
	IModuleClassSpec aus Graph und GraphNode rausziehen: Der Graph soll nur den Namen der
	Module-Klasse speichern. Dann kann man den operator<<() auch wieder verwenden.	

net:
	Das NetProtocol und das Portprotocol irgendwie (ueber ein Template?) "zusammenfaktorisieren".	


model, renderer, dllloader, sequencer:
	Wann duerfen die Methoden die in den interfaces festgelegt sind
	(z.B. IControlValueReceiver::controlValueChanged())
	Exceptions schmeissen? Und wer fängt die ab bzw. leitet
	die Fehlermeldung weiter? (An wen?)

linux:
	fuer modules und types extra projekte machen

ganze engine:
	Interfaces entzerren, vor allem die ControlReceiver Interfaces
	machen zu viel (ModelControlReceiver)

model
	aufteilen in:
	datenbank
		graphen, sequenzen, moduldaten
	steuerung
		weiterleiden von nachrichten je nach zustand (rendergraph editgraph)

module:
	//konverter
	//	number<->color done(coma)

	signalgenerator
		nicht periodisch (trigger ueber flanke am eingang)
			so eine art gaussglocke
	// aviplayer
	//	wenigstens einigermassen echtzeit :) done(georg)
	// dshow output window, also nur win32 momentan, hähähä

skriptsprache:
	machen!

was ich mir dazu selbst wuensche<martin>
  gui:
		besseres layout
			vielleicht Shortcuts um sequenzer auszublenden

		export/import von sequencen/graphen/snapshots
    export/import videos/grafiken

    property/controlls vereinheitlichen gemeinsame codebasis
		

		controlls/properties:
			xfader mit interner rückkopplung für number und position type
			schalter (0,1)
			fontselector (fonts der engine)
			bildselector (bilder auf der engine)
				und natuerlich mit preview :)
			videoselector (videos auf der engine)
				hier muss natuerlich auch eine preview rein :)
      splineeditor für signalgenerator

  engine:
		globale Zeit für alle module
			entweder als eigenes modul oder von der engine bereitgestellt
			oder jedes modul holt sich selbst die systemzeit
			

		module:
			signalgenerator mit periodischen splines
			VideoInput Modul(TV KARTE)
			MIDI input Modul (output ware auch lustig)
			
		typen:
			bilder mit unterschiedlicher groesse
			Fontname
			Bildname
			Videoname

Was ich mir Wünsche <coma>:

-Den Weltfrieden
-Einen Titanium Labtop
-Freiheit für Andreas Baader
-Einen Bierkasten der niemals leer wird
-Eine Kuh die goldene Eier scheisst (Georg)


-pluc.py: richtige code generation optionen fuer release target
	
-keyboard zeug in die .spec files und in die gui einbauen
 Z.B.:

 ...
 inputs
 {
    trigger
    {
	name		  = Trigger
        type              = typ_NumberType
	const             = true
	strong_dependency = true
	widget_type	  = keyboard
	toggle_keys       = f[0,1];q[11,2];...
	keys		  = a[50];...
        ...
    }